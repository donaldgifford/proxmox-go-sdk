---
# markdownlint-cli2 runner options. Lint *rules* live in .markdownlint.yaml
# (this file only scopes which files are linted).
#
# .claude/ holds local tool state — e.g. the donald-loop state file — not repo
# documentation, so it must not be linted or it fails MD041 and friends.
# CHANGELOG.md is generated by git-cliff (cliff.toml); its format is owned by
# the generator and enforced by the Changelog Drift Check, not markdownlint.
# docs/COVERAGE.md is generated by `just coverage`; the coverage drift check
# compares a byte-identical regeneration, so reformatting it would fail CI.
ignores:
  - ".claude"
  - "node_modules"
  - "CHANGELOG.md"
  - "docs/COVERAGE.md"
