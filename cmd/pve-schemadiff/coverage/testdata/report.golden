<!-- Generated by `just coverage`. DO NOT EDIT — regenerate instead. -->

# Proxmox VE API coverage

The SDK reaches **4 of 10 endpoints (40.0%)** in the Proxmox VE 9.9.9 REST API.

The percentage is **not a target**. It is measured against the *entire* REST
API, including surfaces no SDK consumer has asked for; driving it up for its own
sake would mean shipping untested breadth. Read it as a map of what exists, and
use the per-service tables to find whether a specific endpoint is reachable.

| | |
|---|---|
| Measured against | Proxmox VE 9.9.9 (`fixture apidoc.js`, captured 2026-01-01) |
| Denominator | 10 endpoints in `cmd/pve-schemadiff/testdata/baseline.json` |
| Numerator | 6 distinct `mockpve` routes (4 matched a real endpoint) |
| Regenerate | `just coverage` |

A **covered** endpoint has a `mockpve` route, which by this repo's discipline of
testing every op against `mockpve` means an SDK method drives it. A **stub** is a
deliberate `pverr.ErrUnsupported`, with its reason. **Out of scope** is a decision,
with the document that made it. Everything else is a **gap** — including families
nobody has triaged yet.

## Summary

| Service | Covered | Stub | Out of scope | Gap | Total | Share |
|---|--:|--:|--:|--:|--:|--:|
| access | 1 | 0 | 0 | 1 | 2 | 50.0% |
| ceph | 0 | 1 | 0 | 0 | 1 | 0.0% |
| qemu | 2 | 0 | 0 | 1 | 3 | 66.7% |
| version | 1 | 0 | 0 | 0 | 1 | 100.0% |
| unassigned | 0 | 0 | 2 | 1 | 3 | 0.0% |
| **total** | **4** | **1** | **2** | **3** | **10** | **40.0%** |

## Covered outside REST

PVE serves no endpoint for these; the SDK covers them over the `proxmox/ssh`
side channel, so they appear in no table below.

- snippet/backup upload -> /var/lib/vz

## Allowlisted unmatched routes

`mockpve` serves these, but the baseline holds no such endpoint. Each is an
exemption from the fabrication guard, and each is debt: either the baseline is
stale or the route is wrong.

| Route | Reason |
|---|---|
| `GET /cluster/sdn/fabrics` | fixture: baseline predates the fabric collection |

## Per-service detail

### access

1 of 2 covered (50.0%)

| Method | Path | State | Note |
|---|---|---|---|
| GET | `/access/users` | covered |  |
| DELETE | `/access/users/{}` | gap |  |

### ceph

0 of 1 covered (0.0%)

| Method | Path | State | Note |
|---|---|---|---|
| GET | `/nodes/{}/ceph/mirror` | stub | no PVE REST endpoint; rbd CLI over ssh |

### qemu

2 of 3 covered (66.7%)

| Method | Path | State | Note |
|---|---|---|---|
| GET | `/nodes/{}/qemu` | covered |  |
| POST | `/nodes/{}/qemu` | gap |  |
| GET | `/nodes/{}/qemu/{}/config` | covered |  |

### version

1 of 1 covered (100.0%)

| Method | Path | State | Note |
|---|---|---|---|
| GET | `/version` | covered |  |

### unassigned

0 of 3 covered (0.0%)

| Method | Path | State | Note |
|---|---|---|---|
| GET | `/cluster/notifications` | out of scope | owned by the consuming service (ADR-0001) |
| POST | `/cluster/notifications/targets/{}/test` | out of scope | owned by the consuming service (ADR-0001) |
| GET | `/pools` | gap |  |
