---
# mockpve certification record (DESIGN-0002 OQ-8 = b): one entry per recording
# batch. An entry means "mockpve's envelope matches what PVE <pve_version>
# actually returned for the recorded surface" — divergences found while
# comparing mockpve to fresh cassettes are fixed in mockpve (or documented in
# notes) before the entry lands. The committed cassettes replay in CI
# (`just test-replay`), so each batch is regression-guarded, not
# once-observed.
- pve_version: 9.2-1
  recorded: 2026-07-06 # live run on the r740a lab node
  commit: c36b7da # commit that introduced the batch
  harness: branch # pre-v0.2.0 buildout (pvelab did not exist yet)
  cassettes:
    - TestVersionRoundTrip
    - TestComputeReads
    - TestStorageReads
    - TestClusterAndHAReads
    - TestNetworkReads
    - TestAccessReads
    - TestQEMULifecycle
    - TestLXCLifecycle
    - TestISOUpload
    - TestConsoleMint
  notes: >-
    Batch predates the dogfood lab; recorded directly against r740a.
    Divergences reconciled into the SDK/mock from this run and the later
    live spikes: transport chunked-body 501 + redundant-multipart-field 400
    (SDK upload fixes), recorder must not use replayable interactions (task
    polls), and two mockpve fidelity fixes found by the Phase 0/1 pvelab work
    — config reads serialize memory as a quoted string (9.2.4), and the
    qemu/lxc create handlers persist every create-form key like real PVE.
- pve_version: 9.2.2
  recorded: 2026-07-12 # first full pvelab nested-cluster dogfood run
  commit: f6e75ef # code state of the recording run (feat/pvelab-phase5)
  harness: pvelab # 3-node nested cluster on r740a, run-on-host posture
  cassettes:
    - TestResourceAffinityPlacement
  notes: >-
    First batch recorded against the pvelab nested cluster (quorate 3-node,
    PVE 9.2.2) instead of r740a directly — the IMPL-0001 P4 scheduler-observed
    placement criterion needs multiple nodes. Fidelity/API findings reconciled
    before this entry: guest VNC tickets are BOUND to their mint path (mockpve
    now records each ticket's dial path and rejects presentation elsewhere,
    matching live PVE's 401); HA rule updates must re-send the rule type's
    required properties (a bare affinity-only PUT fails parameter
    verification — HARuleUpdate grew Type); HA rule feasibility counts
    HA-ACTIVE nodes, which lag AddResource by LRM cycles (the suite retries
    rule create); and cluster joins need a per-join quorum gate because the
    corosync CONFIG nodelist precedes runtime health (pvelab cluster.go).
    TestConsoleRFB ran in the same batch and verified the WebSocket-framed
    RFB greeting live ("RFB 003.008\n") but has no cassette by design.
- pve_version: 9.1.1
  recorded: 2026-07-13 # version-matrix run — first non-9.2 minor
  commit: 661ddfd # code state of the recording run (main, post-#15)
  harness: pvelab # 9.1 lab via LINKED CLONES (pvelab-tmpl-9-1, 3m11s to quorate)
  cassettes:
    - TestVersionRoundTrip
    - TestComputeReads
    - TestStorageReads
    - TestClusterAndHAReads
    - TestNetworkReads
    - TestAccessReads
    - TestResourceAffinityPlacement
  notes: >-
    First version-matrix batch (IMPL-0002 Phase 5): the suite ran against a
    nested PVE 9.1.1 cluster provisioned via linked clones from the 9.1
    template. NO SDK or mockpve divergences surfaced — reads, placement
    (including the LRM-lag rule-create retry, which fired and settled again),
    and the live RFB greeting were all green on 9.1. The 9.2.7 install
    assistant prepared the 9.1 ISO without issue. The corpus is now
    mixed-version by design: these seven cassettes carry 9.1.1 responses
    while the QEMU/LXC lifecycles, ISO upload, and console mint remain 9.2-1
    (provenance per batch entry). First fully-automated scrub batch — the
    Request.Host topology fix held with zero hand-edits. TestConsoleRFB ran
    live in the batch (cassette-less by design).
- pve_version: 9.2.4
  recorded: 2026-07-22 # TestNetworkReads re-record on the nested fabrics path
  commit: c8f17cf # feat/impl-0004-sdn-fabrics-delivery (PR #21)
  harness: r740a # reads-only, no lab needed
  cassettes:
    - TestNetworkReads
  notes: >-
    Single-cassette re-record (IMPL-0004 Phase 2): the previously committed
    TestNetworkReads interaction hit the old flat /cluster/sdn/fabrics guess,
    whose "passing" read was actually the subdir index. The re-record drives
    the real nested /cluster/sdn/fabrics/fabric path and honestly returns the
    empty fabric list. r740a had moved to 9.2.4 by this recording; the corpus
    stays mixed-version by design.
- pve_version: 9.2.2
  recorded: 2026-07-23 # shared IMPL-0004 + IMPL-0005 Phase-3 pvelab run
  commit: 217fa44 # post-#23 main (HA/SDN-status); fabric re-recorded on fe70f56
  harness: pvelab # 3-node nested cluster via linked clones, quorate
  cassettes:
    - TestHAStatusReads
    - TestHAArmDisarmCycle
    - TestHAResourceMigrate
    - TestSDNStatusReads
    - TestSDNFabricLifecycle
  notes: >-
    The DESIGN-0003/0004 live-verification batch. HA findings reconciled into
    the SDK/mock before this entry: armed-state rides a dedicated "fencing"
    row (never master; a fresh idle cluster has NO master row and reports
    "standby" — the normal state, not a wedged lab), and manager_status NESTS
    its state blob under a manager_status key with a quorum summary alongside
    (quorate as string "1") — ManagerStatus was restructured to the envelope
    and mockpve now emits both shapes. PVE does require disarm's
    resource-mode; a blocked migrate returned blocking-resources with cause
    resource-affinity exactly as apidoc-mined. Fabric: bare-IPv4 ip and
    property-string interfaces both accepted; openfabric NEVER binds an
    address-less bridge-enslaved port (ens18) — enrolling the addressed
    bridge (vmbr0) converged in ~10s (first attempt burned the 3-min ceiling
    at 0 interfaces). Harness fix from the leak review: topologyScrub now
    rewrites go-vcr's parsed Request.Form map (the raw body was scrubbed but
    the form kept the live node name).
